# ATMS Controllers — Corrected Complete Package

Includes authentication, dashboards, taxpayers, users, tax types, tax rules,
declarations, assessments, payments, receipts, notifications, reports,
administration, audit logs, and API endpoints.

Controllers orchestrate requests and authorization. Tax calculation belongs
in `app/services/`, while database operations belong in `app/models/`.
